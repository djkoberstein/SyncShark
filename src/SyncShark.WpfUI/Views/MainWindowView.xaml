<Window x:Class="SyncShark.WpfUI.Views.MainWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"         
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:views="clr-namespace:SyncShark.WpfUI.Views"
        xmlns:viewModels="clr-namespace:SyncShark.WpfUI.ViewModels"
        mc:Ignorable="d" 
        Title="SyncShark" 
        Height="600" 
        Width="525"
        d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <views:ConfigurationView DataContext="{Binding ConfigurationViewModel}"/>
        <views:WorkItemView Grid.Row="1" DataContext="{Binding WorkItemViewModel}"/>
    </Grid>
</Window>
